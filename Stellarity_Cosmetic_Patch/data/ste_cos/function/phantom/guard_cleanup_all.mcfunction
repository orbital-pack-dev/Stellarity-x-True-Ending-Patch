# =====================================================================
# ste_cos:phantom/guard_cleanup_all
# Убивает ВСЕХ фантомов-стражей и их маркеры.
# Вызывается, когда дракона нет (чтобы стражи не спавнились до боя).
# =====================================================================

kill @e[type=phantom,tag=ste_cos_guard]
kill @e[type=marker,tag=ste_cos_guard_marker]
